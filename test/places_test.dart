void main() async {
//  final aPlces = AlgoliaPlaces(apiKey: "");
//  final PlacesAutocompleteResponse result = await aPlces.autocomplete("Miami");
//  result.hits.forEach((Hit hit) => print(hit.suggestion.toString()));
//
}
